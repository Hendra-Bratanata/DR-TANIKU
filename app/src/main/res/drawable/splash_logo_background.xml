<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">

    <!-- Gradient background for logo -->
    <gradient
        android:startColor="@color/primary_green"
        android:centerColor="@color/soil_brown"
        android:endColor="@color/tech_blue"
        android:angle="135" />

    <!-- Border -->
    <stroke
        android:width="4dp"
        android:color="@color/white" />

</shape>